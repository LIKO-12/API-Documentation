{
  "desc":"Transform an image by applying a function to every pixel.",
  "args":[
    {
      "name":"f",
      "type":"function",
      "desc":"The callback function. The x, y coord and color are passed. And the function can return the new pixel color, otherwise the pixel color will stay the same."
    }
  ],
  "note":"The function that's passed as an argument will be called for every pixel in the image, starting with the top-left one, and going in horizental lines."
}
