{
  "desc":"This function will multiply the image dimensions with the given scale, and automatically scales the image data, then returns the result imagedata.",
  "args":[
    {
      "name":"scale",
      "type":"number",
      "desc":"The scale to multiply the image dimensions with, must be bigger than zero and integer."
    }
  ],
  "rets":[
    {
      "name":"enimgdata",
      "type":"imagedata",
      "desc":"The resulting imagedata."
    }
  ]
}
