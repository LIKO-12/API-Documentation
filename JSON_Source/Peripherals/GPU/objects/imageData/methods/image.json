{
  "desc":"Converts the imagedata to an image, which can be drawn to the screen.",
  "rets":[
    {
      "name":"img",
      "type":"image",
      "desc":"The created image object."
    }
  ],
  "note":"This function is slow if called repeatly, unlike image:data(). image:refresh() will reload it's content from it's source imagedata."
}
